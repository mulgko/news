[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python main.py"
sourceDir = "server-python"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[build.env]
NODE_ENV = "production"

[deploy.env]
PORT = "8080"
