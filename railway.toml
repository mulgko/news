[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run build:client && python server-python/main.py"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[build.env]
NODE_ENV = "production"

[deploy.env]
PORT = "8080"
